{
  "name": "find-my-person-portal",
  "version": "1.0.0",
  "description": "Missing Persons Portal Monorepo",
  "main": "backend/server.js",
  "scripts": {
    "start": "node backend/server.js",
    "build": "cd frontend && npm install && npm run build && cd ../backend && npm install"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
